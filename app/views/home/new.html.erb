</br>
<div id="topicList">
  <table class="tableTopicList">
          <thead>
                  <tr><th>主題</th><th>發問者</th></tr>
          </thead>
	   <tbody>
                  <% @showFeedbacks.each do |show| -%>
                          <tr>
				<td><%=link_to show.topic, home_path(show)%></td>
				<td><%=show.name%></td>
			  </tr>
                  <% end-%>
          </tbody>
  </table>
</div>

</br>
</br>
</br>

<div id='feedbackForm'>
  <font face="微軟正黑體" size="5"><b>我有問題要問！</b></font>
  <font face="微軟正黑體">
  <%= form_tag '/home', :method=>nil  do %>
        <%= label :feedback, :name, '名稱'%>
        <%= text_field :feedback, :name%>

        <%= label :feedback, :phone,'電話'%>
        <%= text_field :feedback, :phone%>

        <%= label :feedback, :email,'email'%>
        <%= text_field :feedback, :email%>

        <%= label :feedback, :topic,'主題'%>
        <%= text_field :feedback, :topic%>

        <%= label :feedback, :context, '問題'%>
        <%= text_area :feedback, :context%>

        <%= submit_tag '確定送出'%>
  <% end%>
  </font>
</div>
